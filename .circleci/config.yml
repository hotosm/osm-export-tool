version: 2
jobs:
  build:
    docker:
      - image: mojodna/cci-docker-compose
    working_directory: ~/
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: Run tests
          command: |
            set -x
            docker-compose -f docker-compose.test.yml run site make test
